#include <bits/stdc++.h>
using namespace std;

int main(){
    // defining array
    int arr[5];

    // Taking input array elements
    cout<<"Enter the element of array of size array[5]: ";
    for(int i = 0 ; i < 5; i++){
        cin>>arr[i];
    }

    cout<<"Elements in your array are arrat[5]: ";
    for(int j = 0; j < 5; j++){
        cout<<arr[j]<<" ";
    }
}
